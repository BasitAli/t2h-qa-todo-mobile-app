import {
  View,
  ScrollView,
  Text,
  TouchableOpacity as TouchableWithoutFeedback,
  TextInput
} from "react-native";
import Button from "./Button";

export { Text, View, ScrollView, TouchableWithoutFeedback, Button, TextInput };
